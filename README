[Global]
sudo apt-get install git-core
git config --global user.name "valery1707"
git config --global user.email "valery1707___"

[Create repo]
cd ~
git init
echo '*' > .gitignore
>>[Add remote]

[Use repo]
cd ~
git init
>>[Add remote]
>>[Pull from github]

[Add remote]
#Work over HTTPS
git remote add origin https://valery1707@github.com/valery1707/test.git
#Work over SSH (will use ~/.ssh/id_rsa and ~/.shh/id_rsa.pub, read: http://help.github.com/msysgit-key-setup/)
git remote add origin git@github.com:valery1707/dotfiles-test.git

[Change]
git add -f new_file_name.ext
git commit -m 'comment for commit'

[Push to github]
git push origin master

[Pull from github]
git pull origin master
